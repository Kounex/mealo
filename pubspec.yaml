name: mealo
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

# Globals for packages which share the same version
isar_version: &isar_version 3.0.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # Unique identifier for our models
  uuid: ^3.0.7

  # Immutability
  freezed: ^2.3.2
  freezed_annotation: ^2.2.0

  # JSON parser and serializer
  crimson: ^0.2.1

  # State management
  flutter_riverpod: ^2.1.3
  riverpod_annotation: ^1.1.1

  # Local storage
  isar: *isar_version
  isar_flutter_libs: *isar_version
  shared_preferences: ^2.0.17

  # Navigator 2.0
  beamer: ^1.5.3

  # Some fancy utility functions for collections
  collection: ^1.17.0

  # Enable camera access and handling
  camera: ^0.10.3

  # Enable selecting files
  file_picker: ^5.2.5

  # UI
  # Consistent theming
  flex_color_scheme: ^6.1.2

  # Power up slivers
  sliver_tools: ^0.2.8

  # Animations
  flutter_staggered_animations: ^1.1.1

  # Microsoft icons pack
  fluentui_system_icons: ^1.1.190

  # Adjust text size correctly
  auto_size_text: ^3.0.0

  # Loading shimmering
  shimmer: ^2.0.0

  # Powered-up animations
  flutter_animate: ^4.0.0

  # Pre-built color picker because it is a pain in the ass to do on my own
  flutter_colorpicker: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

  # analyzer: ^5.5.0
  # analyzer_plugin: ^0.11.2
  custom_lint: ^0.2.5
  riverpod_lint: ^1.0.1

  # Generator stuff
  isar_generator: *isar_version
  riverpod_generator: ^1.1.1
  build_runner: ^2.3.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/
